{
  "name": "lkoell/symfony-twig-scoped-cache",
  "description": "Scoped twig template cache. Useful if you run many domains/clients with separate templates but don't want to clear all template caches just to update one domain/client.\n",
  "require": {
    "php": "^7.2",
    "symfony/twig-bundle": "^4.0"
  },
  "autoload": {
    "psr-4": {
      "LkScopedCache\\": "src/LkScopedCache"
    }
  }
}
